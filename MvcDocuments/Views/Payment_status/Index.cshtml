@model IEnumerable<MvcDocuments.Models.Payment_status>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Payment_status_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Payment_status_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Payment_statusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.Payment_statusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Payment_statusId })
        </td>
    </tr>
}

</table>
